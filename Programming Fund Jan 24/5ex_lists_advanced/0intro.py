# Sort elements in descending order
# numbers_list = [6, 2, 1, 4, 3, 5]
# sorted_numbers = sorted(numbers_list, key=lambda x: -x)
# # sorted_numbers = sorted(numbers_list, reversed=True)

# Swap elements of list
# nums = [1, 2, 3]
# nums[0], nums[1], nums[2] = nums[2], nums[0], nums[1]

# words = ['python', 'programming', 'is, 'fun']
# sorted_by_length = sorted(words, key=len, reverse=true)

# Filter with .startswith
# names = ['Tim', 'Dim', 'Sim', 'Tis']
# filtered_names = filter(lambda name: name.startswith('T'), names)
# print(list(filtered_names))

# Generate a pyramid pattern with asterisks
